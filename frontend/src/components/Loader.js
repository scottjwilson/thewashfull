import React from "react";

const Loader = () => {
  return (
    <div className="h-64 flex items-center justify-center bg-blue-200 animate-pulse">
      <span>loading..</span>
    </div>
  );
};

export default Loader;
